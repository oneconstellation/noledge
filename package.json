{
	"scripts": {
		"build": "cp index.html dist/ && esbuild --bundle index.tsx --outdir=dist/js --minify --sourcemap",
		"start": "pnpm build --servedir=dist"
	},
	"dependencies": {
		"react": "^18.2.0",
		"react-dom": "^18.2.0",
		"styled-components": "^5.3.5"
	},
	"devDependencies": {
		"esbuild": "^0.14.47",
		"typescript": "^4.7.4"
	}
}
